import { createStore } from 'vuex';

const store = createStore({
  state() {
    return {
      // Définissez ici l'état initial
    };
  },
  mutations: {
    // Définissez ici les mutations pour modifier l'état
  },
  actions: {
    // Définissez ici les actions pour exécuter des tâches asynchrones
  },
  getters: {
    // Définissez ici les getters pour accéder à l'état
  },
});

export default store;
